interface:
  display_name: "User Flow Map"
  short_description: "Map flow surfaces, channels, branches, states, and low-fi UI-candidate notes"
  default_prompt: "Use /user-flow-map to turn this positioned product or feature goal into flow surface structure, channels, decisions, branches, states, failure paths, and low-fidelity notes for UI candidates before UX/UI requirements and layout variants."

policy:
  allow_implicit_invocation: true
