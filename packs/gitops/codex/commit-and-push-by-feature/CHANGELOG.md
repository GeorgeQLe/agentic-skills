# Changelog

## v0.1 - 2026-05-30

- Added the pack install artifact boundary so `.agents/project.json` is committed for pack configuration changes while generated `.claude/skills/**` and `.codex/skills/**` roots are never bucketed, staged, or committed.

## v0.0

- Initial Codex feature-bucket commit and push workflow.
