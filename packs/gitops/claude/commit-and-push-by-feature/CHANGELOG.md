# Changelog

## v0.2 - 2026-07-12

- Converted the workflow into a compatibility wrapper over issue-backed non-primary branches, safe publication, and ready pull-request upsert without automatic merge.

## v0.1 - 2026-05-30

- Added the pack install artifact boundary so `.agents/project.json` is committed for pack configuration changes while generated `.claude/skills/**` and `.codex/skills/**` roots are never bucketed, staged, or committed.

## v0.0

- Initial Claude feature-bucket commit and push workflow.
