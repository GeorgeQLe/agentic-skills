interface:
  display_name: "Mono Guard"
  short_description: "Validate branch-backed monorepo lane boundaries"
  default_prompt: "Use $mono-guard to validate .agents/lane-specs.json before dispatch or audit integrated branch-backed lane changes with --post-integration."

policy:
  allow_implicit_invocation: true
