# Changelog

## v0.1

- Renamed the Claude exec-loop command from `/run` to `/exec` to avoid collision with Claude's default slash-command surface.
- Preserved the existing execution behavior, plan-mode approval contract, and `/ship` handoff.

## v0.0

- Initial exec-loop `run` skill contract.
