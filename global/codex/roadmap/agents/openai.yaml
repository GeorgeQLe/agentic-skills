interface:
  display_name: "Roadmap"
  short_description: "Scan task pipeline health, build or update the project roadmap, and maintain a priority task queue"
  default_prompt: "Use $roadmap to scan task pipeline state, build or update the roadmap, and maintain the priority task queue in tasks/todo.md."

policy:
  allow_implicit_invocation: true
