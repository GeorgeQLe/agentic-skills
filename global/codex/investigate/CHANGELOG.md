# Changelog

## v0.1

- Require `$investigate` alignment pages to automatically attempt copying compiled YAML, show copy status, and provide an explicit `Copy YAML` fallback button.

## v0.0

- Initial `$investigate` skill contract.
