interface:
  display_name: "Investigate"
  short_description: "Trace bug to root cause"
  default_prompt: "Use $investigate to investigate this bug and find the root cause."

policy:
  allow_implicit_invocation: true
