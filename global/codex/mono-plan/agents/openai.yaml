interface:
  display_name: "Mono Plan"
  short_description: "Generate monorepo-safe parallel lane specs"
  default_prompt: "Use $mono-plan to generate safe parallel lane specs for this monorepo phase."

policy:
  allow_implicit_invocation: true
