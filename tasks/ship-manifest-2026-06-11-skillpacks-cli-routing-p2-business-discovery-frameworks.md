# Ship Manifest — Skillpacks CLI Routing P2 Business-Discovery Frameworks

- **User goal:** Execute the next `$exec` step: remediate nested business-discovery framework install-routing wording so users see npm `skillpacks` install alternatives alongside runner-local routes.
- **Changed files:** Nested `packs/business-discovery/{claude,codex}/competitive-analysis/frameworks/{feature-pricing-matrix,porter-five-forces,strategic-group-map,swot}/SKILL.md`; nested `packs/business-discovery/{claude,codex}/customer-discovery/frameworks/{five-rings,four-forces,jtbd-needs,pmf-engine,seven-dimensions,w3-hypothesis}/SKILL.md`; nested `packs/business-discovery/{claude,codex}/positioning/frameworks/{category-design,jtbd-positioning,moore-positioning,obviously-awesome,strategic-canvas}/SKILL.md`; matching `CHANGELOG.md` files; matching archive snapshots under `archive/<old-version>/`; Skills Showcase generated proof assets; `tasks/todo.md`; `tasks/history.md`; this manifest; `prompts/exec/skill-prompt-20260610-220256-exec.md`.
- **Per-file purpose:** Active framework skill files now preserve `/pack install` or `$pack install` while adding `npx skillpacks install <pack>` project-shell alternatives; changelogs and archives satisfy skill versioning; generated proof assets publish current history/fingerprints; task/history/manifest files record execution and next work; prompt history captures the visible invocation.
- **User-goal mapping:** The targeted audit report no longer lists any nested business-discovery framework target, reducing expected active install-routing findings from 186 to 156 and leaving business-growth plus later P2/P3 buckets for follow-up.
- **Tests run:** `node apps/skills-showcase/scripts/generate-skills-showcase-data.mjs`; `node apps/skills-showcase/scripts/generate-skills-showcase-github-data.mjs`; `apps/skills-showcase/scripts/validate-skills-showcase-data.sh`; `bash -n scripts/skill-install-routing-audit.sh`; `scripts/skill-install-routing-audit.sh --fixtures tests/fixtures/skill-install-routing`; `bash scripts/skill-pack-routing-audit.sh`; `bash scripts/skill-versions.sh --missing`; `bash scripts/skill-archive-audit.sh --strict`; `bash scripts/skill-deps.sh --broken`; `node scripts/upgrade-alignment-page.mjs --check`; `pnpm --dir tests exec vitest run --project layer1 layer1/skill-install-routing-audit.test.ts`; `pnpm --dir apps/skills-showcase build`; `git diff --check`.
- **Skipped tests:** Full layer1 was not rerun because the behavior changed only skill install-route wording and generated proof metadata; focused install-routing layer1 plus skill archive/version/dependency/generated-data/build checks cover the affected contracts.
- **Expected failures:** `scripts/skill-install-routing-audit.sh --active` exits 1 with 156 findings. This is expected remaining P2/P3 install-routing debt after this slice, not a regression.
- **Adversarial review:** The report-mode gate was filtered for all nested business-discovery framework target paths and returned no matches; strict active mode confirmed the remaining findings now start with `business-growth`; diff review verified 30 active framework skills, 30 changelog entries, and 30 new archive snapshots. The review also caught and fixed an initial miss in the 12 customer-discovery changelogs before final validation.
- **Residual risk:** The wording migration is still incomplete across business-growth and later P2/P3 buckets. Until those ship, strict active audit remains expected-red.
- **Rollback note:** Revert the shipping commit to restore the prior framework wording, generated proof assets, and task docs. If rolling back manually, also remove the new archive snapshots and restore the previous active versions/changelog entries.
- **Next command:** `$exec`
