# Ship Manifest — Skillpacks CLI Routing P2 Business-Discovery Top-Level

- **User goal:** Execute the next `$exec` step: remediate the top-level business-discovery P2 install-routing wording so users see npm `skillpacks` install alternatives alongside runner-local routes.
- **Changed files:** Top-level `packs/business-discovery/{claude,codex}/{competitive-analysis,customer-discovery,customer-feedback,enterprise-icp,lean-canvas,positioning,value-prop-canvas}/SKILL.md`; matching `CHANGELOG.md` files; matching archive snapshots under `archive/<old-version>/`; Skills Showcase generated data/proof assets; `tasks/todo.md`; `tasks/roadmap.md`; `tasks/history.md`; this manifest; `prompts/exec/skill-prompt-20260611-015300-exec.md`.
- **Per-file purpose:** Active skill files now preserve `/pack install` or `$pack install` while adding `npx skillpacks install <pack>` project-shell alternatives; changelogs and archives satisfy skill versioning; generated showcase assets publish new versions/fingerprints; task/history/manifest files record execution and next work; prompt history captures the visible invocation.
- **User-goal mapping:** The targeted audit report no longer lists any top-level business-discovery target, reducing expected active install-routing findings from 200 to 186 and leaving nested frameworks plus later P2/P3 buckets for follow-up.
- **Tests run:** `node apps/skills-showcase/scripts/generate-skills-showcase-data.mjs`; `node apps/skills-showcase/scripts/generate-skills-showcase-github-data.mjs`; `apps/skills-showcase/scripts/validate-skills-showcase-data.sh`; `bash -n scripts/skill-install-routing-audit.sh`; `bash scripts/skill-install-routing-audit.sh --fixtures tests/fixtures/skill-install-routing`; `bash scripts/skill-pack-routing-audit.sh`; `bash scripts/skill-versions.sh --missing`; `bash scripts/skill-archive-audit.sh --strict`; `bash scripts/skill-deps.sh --broken`; `node scripts/upgrade-alignment-page.mjs --check`; `pnpm --dir tests exec vitest run --project layer1 layer1/skill-install-routing-audit.test.ts`; `pnpm --dir apps/skills-showcase build`; `git diff --check`.
- **Skipped tests:** Full layer1 was not rerun because the behavior changed only skill install-route wording and generated showcase metadata; focused install-routing layer1 plus skill archive/version/dependency/generated-data/build checks cover the affected contracts.
- **Expected failures:** `bash scripts/skill-install-routing-audit.sh --active` exits 1 with 186 findings. This is expected remaining P2/P3 install-routing debt after this slice, not a regression.
- **Adversarial review:** The report-mode gate was filtered for all top-level business-discovery target paths and returned no matches; strict active mode confirmed the remaining findings start with nested business-discovery framework skills; generated showcase diff was reviewed to confirm only the 14 business-discovery versions and fingerprints changed; `apps/skills-showcase/next-env.d.ts` build-mode churn was restored.
- **Residual risk:** The wording migration is still incomplete across nested business-discovery frameworks and later P2/P3 buckets. Until those ship, strict active audit remains expected-red.
- **Rollback note:** Revert the shipping commit to restore the prior business-discovery wording, generated assets, and task docs. If rolling back manually, also remove the new archive snapshots and restore the previous active versions/changelog entries.
- **Next command:** `$exec`
