# Ship Manifest — Unified Experience Phase 2: Real Deck Data Model

**Date:** 2026-06-19
**User goal:** Kill the deck-builder's invented 7 `SETS` + `LAB-01..07` round-robin and render the 5 canonical manifest decks (VARD, ORD, Business/Devtool/Game AFPS) with real named phase→card chains, by extending the skillpacks manifest.

## Changed files (shipping boundary)

| File | Purpose |
| --- | --- |
| `packages/skillpacks/scripts/build-skillpacks-manifest.mjs` | Add `phases:[{key,name,cards}]` to all 5 deck defs; carry into built deck; extend `validateManifest` to assert each phase card resolves to an installable skill in a deck `full_pack`. |
| `packages/skillpacks/dist/skillpacks-manifest.json` | Regenerated (generated artifact, not hand-edited). |
| `apps/skills-showcase/scripts/generate-skills-showcase-data.mjs` | Read manifest from git index; emit `decks[]`/`sets[]`; drop unused `workflows`; add manifest to `sourcePaths`. |
| `apps/skills-showcase/src/deck-builder/decks.ts` | Delete `SETS`/`SetDef`/`getSetSkills`/round-robin; new `Phase` type + membership data model; `buildDecks(data)`. |
| `apps/skills-showcase/src/hooks/useSkillsData.ts` | `SkillsData.decks`/`sets`; export generated types; remove now-dead `getPackSkills`/`getGlobalSkills`. |
| `apps/skills-showcase/src/deck-builder/DeckTableShell.tsx` | `Phase[]` consumption; membership-based `slotColumnIndex`/`targetPhaseId`/`wantedIds`; slot render by `phase.key`/`phase.name`. |
| `apps/skills-showcase/app/prototype/page.tsx` | Repoint at `buildDecks(data)` (one SealedPack per deck) after `SETS` removal. |
| `apps/skills-showcase/src/deck-builder/DeckTableShell.test.tsx` | `vard` fixture deck; mock returns `decks`/`sets`; dead `getPackSkills` mock dropped. |
| `apps/skills-showcase/e2e/deck-table-shell.spec.ts` | `SLUG` `market-intel`→`vard` (real 3-card deck). |
| `apps/skills-showcase/src/components/prototype-close-sequence.test.tsx` | Mock gains a generated-deck fixture; dead mocks dropped. |
| `apps/skills-showcase/public/assets/skills-data.js`, `docs/skills-showcase/assets/skills-data.js` | Regenerated showcase data (real decks/sets). |
| `apps/skills-showcase/public/assets/github-proof-data.js`, `docs/skills-showcase/assets/github-proof-data.js`, `docs/benchmark-results-matrix.md` | Regenerated by the same `generate:data`/`validate:data` run (fingerprint/date refresh); required for the freshness gate to pass on a clean checkout. |

## User-goal mapping
Manifest `phases` (machine truth) + generator `decks[]`/`sets[]` + `decks.ts` membership model + `DeckTableShell` consumption together replace round-robin with the real workflow chains. `validateManifest` guards card correctness.

## Tests run (executable verification)
- `npm run typecheck` — clean.
- `node packages/skillpacks/scripts/build-skillpacks-manifest.mjs --check` — passes (new phase-card validation green).
- Vitest — 171/171.
- Playwright `test:e2e` — 18/18 against real `vard`.
- `npm run validate:data` — fresh.
- skillpacks `npm run build:check` — passes.

## Skipped tests
None.

## Adversarial review
Medium-effort review (1 correctness finder + 1 cleanup/convention finder, both via subagents). Correctness: 0 findings (verified `targetPhaseId` never derefs `undefined` because `buildDecks` drops empty decks and `deck.skills` is the phase union; claude-variant id resolution confirmed; no inverted `wantedIds`). Cleanup: dead `getPackSkills`/`getGlobalSkills` exports + stale test mocks — **fixed**. No CLAUDE.md violations (git-index rule satisfied: manifest read via `readTextFromIndex`, added to `sourcePaths`, generated assets committed atomically with source).

## Residual risk
The phase shape `{key,name,cards/suggestedCardIds}` is hand-declared in three places — the manifest build script (`.mjs`), the generator (`.mjs`), and the TS `GeneratedPhase`/`GeneratedDeck` interfaces — with no shared type. Both scripts are untyped by nature; a future field addition won't fail fast across the boundary. Accepted for this phase; a shared schema would be a separate refactor.

## Rollback note
Revert this commit and rerun `node packages/skillpacks/scripts/build-skillpacks-manifest.mjs` + `npm run generate:data` to restore the prior `SETS`/round-robin data.

## Next command
`/exec` — continue the unified-experience build (Phase 3: `/card/[id]`), NOT started this session per the ship-one-step handoff.
