# Ship Manifest — Business Discovery PACK Customer-Discovery Rename

## User Goal

Execute the next incomplete `$exec` step: Phase 4.2, update `packs/business-discovery/PACK.md` for the `icp` to `customer-discovery` rename, validate it, and ship the result.

## Changed Files

- `packs/business-discovery/PACK.md`
- `docs/skills-showcase/assets/skills-data.js`
- `apps/skills-showcase/public/assets/skills-data.js`
- `docs/skills-showcase/assets/github-proof-data.js`
- `apps/skills-showcase/public/assets/github-proof-data.js`
- `docs/benchmark-results-matrix.md`
- `tasks/todo.md`
- `tasks/history.md`
- `tasks/ship-manifest-2026-06-06-business-discovery-pack-rename.md`
- `prompts/exec/skill-prompt-20260606-223254-exec.md`

## Per-File Purpose

- `packs/business-discovery/PACK.md`: replace legacy executable `icp` naming in the pack overview, default flow, and skill list with `customer-discovery`.
- `docs/skills-showcase/assets/skills-data.js` and `apps/skills-showcase/public/assets/skills-data.js`: regenerated static showcase data after the tracked pack metadata change.
- `docs/skills-showcase/assets/github-proof-data.js` and `apps/skills-showcase/public/assets/github-proof-data.js`: regenerated proof data to keep static proof assets fresh.
- `docs/benchmark-results-matrix.md`: regenerated by the showcase data script, updating the generated date only.
- `tasks/todo.md`: mark Phase 4.2 complete, record validation notes, and add a self-contained plan for Phase 4.3.
- `tasks/history.md`: record the shipped Phase 4.2 outcome.
- `tasks/ship-manifest-2026-06-06-business-discovery-pack-rename.md`: document the quality gate for this shipping boundary.
- `prompts/exec/skill-prompt-20260606-223254-exec.md`: capture the visible `$exec` invocation per repository prompt-history rules.

## User-Goal Mapping

- The PACK.md edit satisfies the requested next incomplete execution step.
- The generated assets satisfy the `$exec` requirement to refresh Skills Showcase data when a tracked `PACK.md` changes.
- The task/history/manifest/prompt files satisfy the repository orchestration and shipping contracts for `$exec`.

## Tests Run

- `rg -n "icp|/icp|\\$icp|icp-needed" packs/business-discovery/PACK.md` — passed; only the intentional `enterprise-icp` skill-list entry remains.
- `node scripts/generate-skills-showcase-data.mjs` — passed; wrote Skills Showcase data and benchmark matrix.
- `node scripts/generate-skills-showcase-github-data.mjs` — passed; wrote proof data.
- `scripts/validate-skills-showcase-data.sh` — passed; generated data is fresh.
- `pnpm --dir apps/skills-showcase build` — passed; Next.js production build completed with TypeScript and static generation.
- `git diff --check` — passed.

## Skipped Tests

- Full `pnpm --dir tests test` was not run for this narrow pack-metadata boundary because the changed executable surface is generated static showcase data, covered by the showcase validator and app production build. The current task ledger also documents pre-existing unrelated layer1 failures in the full suite from the customer-discovery rename; this step did not touch those contracts.

## Adversarial Review

Method: changed-file self-review plus targeted active-reference scan and generated-diff review.

Findings:
- `customer-discovery` exists in both `packs/business-discovery/codex/` and `packs/business-discovery/claude/`, so the PACK.md skill list matches actual roots.
- The remaining `icp` substring in `PACK.md` is only `enterprise-icp`, which should remain unchanged.
- Generated data diffs are expected fingerprints, proof-history ordering, and generated-date changes from the required refresh.
- `pnpm --dir apps/skills-showcase build` temporarily flipped `apps/skills-showcase/next-env.d.ts` from dev route types to production route types; this was build-mode verification churn and was restored before staging.

No additional fixes were needed.

## Residual Risk

Other global and pack skills still contain legacy `icp` command routes by design; those are explicitly queued as later Phase 4 steps and were outside this narrow PACK.md shipping boundary.

## Rollback Note

Revert the shipping commit, or restore `packs/business-discovery/PACK.md` to the prior `icp` wording and rerun the two showcase generators plus `scripts/validate-skills-showcase-data.sh`.

## Next Command

`$exec`
