# Ship Manifest - skillpacks 0.1.22-experimental.1 canary closeout

- **User goal:** Close out the already-published canary release `0.1.22-experimental.1` for both npm package names by recording matching source state, tagging it, pushing it, and verifying the published packages without moving `latest`.
- **Changed files:** `packages/skillpacks/package.json`; `packages/skillpacks/dist/skillpacks-manifest.json`; `prompts/ship-end/skill-prompt-20260708-093240-canary-closeout.md`; `tasks/todo.md`; `tasks/roadmap.md`; `tasks/history.md`; this manifest.
- **Per-file purpose:** `package.json` records the canary prerelease source version; `skillpacks-manifest.json` records the matching `canary` package metadata and canary-only manifest entries; the prompt log captures the visible invocation; task docs and history record the closeout evidence; this manifest records the shipping boundary.
- **User-goal mapping:** Source-state commit `6de131fdf4bc9dee16444d410c565b37e62be3bf` matches the immutable npm canary version. Tag `skillpacks-v0.1.22-experimental.1` points at that source-state commit. npm `experimental` points to `0.1.22-experimental.1` for both package names while npm `latest` remains `0.1.21`.
- **Tests run:** `node -e "...metadata check..."` confirmed `packages/skillpacks/package.json` version and manifest package metadata; `SKILLPACKS_PACKAGE_LANE=canary npm --workspace packages/skillpacks run build:check`; `git diff --check`; `git diff --cached --check`; `npm view skillpacks dist-tags --json`; `npm view @glexcorp/gskp dist-tags --json`; `npx -y --package skillpacks@experimental -- skillpacks list`; `npx -y --package @glexcorp/gskp@experimental -- skillpacks list`; `npx -y --package skillpacks@experimental -- skillpacks install code-quality`; `npx -y --package skillpacks@experimental -- skillpacks install quality-sweep`; `git tag --list 'v0.1.22'`; `git rev-parse skillpacks-v0.1.22-experimental.1`; `node scripts/audit-task-docs.mjs`.
- **Skipped tests:** Full package unit tests were not rerun because this closeout changes package release metadata and a generated lane manifest, not CLI implementation behavior. The executable proof needed here is canary-lane build determinism, npm dist-tag parity, and published package smoke coverage.
- **Adversarial review:** Checked that the manifest was regenerated after staging the package version so its index-derived fingerprint matches the source-state commit. Verified the canary manifest intentionally adds only canary-lane briefing-slide entries beyond metadata/fingerprint changes. Verified no local stable `v0.1.22` tag exists and both npm package names still keep `latest` on `0.1.21`.
- **Residual risk:** Low. The npm packages were already published before this closeout; this work reconciles git source/tag state and independently smoke-tests the published canary channel from disposable directories.
- **Rollback note:** Do not unpublish npm packages. If the git closeout must be reverted, revert the canary source-state commit and docs commit, delete or retarget `skillpacks-v0.1.22-experimental.1`, and document that git no longer mirrors the immutable canary registry state.
- **Next command:** `$ship-end`
