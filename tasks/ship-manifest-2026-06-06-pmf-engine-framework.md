# Ship Manifest — customer-discovery pmf-engine framework

## User goal

Execute the next `$exec` step from `tasks/todo.md`: finish Phase 3 by adding the missing `pmf-engine` customer-discovery framework subskill mirrors, then ship the result and prepare the next step.

## Changed files

- `packs/business-discovery/claude/customer-discovery/frameworks/pmf-engine/SKILL.md`
- `packs/business-discovery/claude/customer-discovery/frameworks/pmf-engine/CHANGELOG.md`
- `packs/business-discovery/claude/customer-discovery/frameworks/pmf-engine/ALIGNMENT-PAGE.md`
- `packs/business-discovery/codex/customer-discovery/frameworks/pmf-engine/SKILL.md`
- `packs/business-discovery/codex/customer-discovery/frameworks/pmf-engine/CHANGELOG.md`
- `packs/business-discovery/codex/customer-discovery/frameworks/pmf-engine/ALIGNMENT-PAGE.md`
- `tests/harness/bench-coverage.ts`
- `docs/skills-showcase/assets/skills-data.js`
- `apps/skills-showcase/public/assets/skills-data.js`
- `docs/skills-showcase/assets/github-proof-data.js`
- `apps/skills-showcase/public/assets/github-proof-data.js`
- `docs/benchmark-results-matrix.md`
- `prompts/exec/skill-prompt-20260606-181815-exec.md`
- `tasks/todo.md`
- `tasks/history.md`
- `tasks/ship-manifest-2026-06-06-pmf-engine-framework.md`

Separate already-shipped concurrent docs commit:

- `05204692 docs: record skills-as-scripts investigation` preserved unrelated task-doc and prompt-history work that appeared during this session before this PMF Engine boundary was staged.

## Per-file purpose

- `pmf-engine/SKILL.md` mirrors define the Vohra/Supan PMF Engine subskill for product-exists customer discovery with real-user evidence, PMF signal, very-disappointed segmentation, and High-Expectation Customer synthesis.
- `pmf-engine/CHANGELOG.md` mirrors record the initial v0.0 subskill version.
- `pmf-engine/ALIGNMENT-PAGE.md` mirrors are generated from the canonical alignment-page convention.
- `tests/harness/bench-coverage.ts` records `pmf-engine` as blocked coverage, matching the other customer-discovery framework subskills that lack deterministic fixtures.
- Generated Skills Showcase assets were refreshed because tracked skill metadata changed; the generator intentionally excludes nested framework subskills but also caught stale top-level `user-flow-map` catalog data.
- `docs/benchmark-results-matrix.md` was regenerated by `scripts/generate-skills-showcase-data.mjs`.
- `prompts/exec/...` captures the visible `$exec` invocation.
- `tasks/todo.md` marks Phase 3 complete and records a self-contained Phase 4.1 plan.
- `tasks/history.md` records the completed step.
- This manifest records the quality gate and shipping boundary.

## User-goal mapping

- Phase 3 required every framework scaffold to have `SKILL.md`, `CHANGELOG.md`, and `ALIGNMENT-PAGE.md` in Claude and Codex mirrors; the PMF Engine files close the last unchecked framework item.
- The benchmark coverage update prevents the new repository skill name from breaking coverage validation.
- Generated asset refresh satisfies the skill-change quality gate.
- Task/history updates make the next `$exec` invocation continue from Phase 4 documentation updates.

## Tests run

- `node scripts/upgrade-alignment-page.mjs` — wrote the two new `pmf-engine/ALIGNMENT-PAGE.md` files.
- `node scripts/generate-skills-showcase-data.mjs` — regenerated showcase skill data and `docs/benchmark-results-matrix.md`.
- `node scripts/generate-skills-showcase-github-data.mjs` — regenerated showcase GitHub proof data.
- `scripts/validate-skills-showcase-data.sh` — passed on second pass after generated assets were updated.
- `scripts/skill-versions.sh --missing` — passed, all 395 skills have version fields.
- `scripts/skill-archive-audit.sh --strict` — passed, 357 active skills checked, 0 violations.
- `scripts/skill-deps.sh --broken` — passed, no broken references found.
- `scripts/skill-pack-routing-audit.sh` — passed, no cross-pack recommendation gaps found.
- `node scripts/upgrade-alignment-page.mjs --dry-run` — passed, 0 updates.
- `pnpm --dir tests bench:coverage` — passed, benchmark coverage matrix valid for 181 skills.
- `node -e ...` staged-research string check for both PMF Engine mirrors — passed.
- Targeted `rg` adversarial scans over PMF Engine mirrors and benchmark coverage — passed; Codex uses `$` routes, Claude uses slash routes, output paths and constraints are present.
- `git diff --check` — passed.
- `pnpm --dir tests verify > /tmp/agentic-skills-verify-20260606-181815.log` — failed with 46 known layer1 failures unrelated to this PMF Engine diff; final failure summary does not list `pmf-engine`.

## Skipped tests

- Full all-layer benchmark/live suites were not run because this step changes skill contracts, generated metadata, and coverage registry only; `pnpm --dir tests bench:coverage` and repository skill integrity checks cover the changed executable surfaces.
- `pnpm --dir tests verify` was run and still fails on pre-existing layer1 drift documented in `tasks/todo.md`; broader reruns would not isolate this diff until the existing rename/alignment test debt is fixed.
- No deploy was run because this step changes repository skill/documentation metadata only, not runtime deploy behavior.

## Adversarial review

- Checked whether PMF Engine improperly supports pre-product mode; contract now requires real user/customer evidence and stops when unavailable.
- Checked mirror route syntax; Claude mirror uses `/pack` and `/customer-discovery`, Codex mirror uses `$pack` and `$customer-discovery`.
- Checked that the subskill does not emit downstream routing; it stops at approval/research output like sibling framework subskills.
- Initial verification exposed missing benchmark coverage and exact staged-research strings; both were fixed before final validation.
- Confirmed showcase generator excludes nested framework subskills by regex, so generated showcase assets do not contain `pmf-engine`; benchmark coverage still tracks it as a repository skill.

## Residual risk

- PMF Engine has blocked benchmark coverage rather than a deterministic runnable fixture. This is intentional because the workflow needs real product/user evidence; follow-up can build a synthetic customer-evidence fixture via `$targeted-skill-builder pmf-engine benchmark coverage`.
- Broad layer1 verification remains red due to existing repository drift unrelated to this diff. The next relevant cleanup is the ongoing Phase 4 icp → customer-discovery documentation and route cleanup.
- Generated showcase assets include stale `user-flow-map` catalog catch-up from prior shipped work. This is acceptable because the generator is the canonical source and the freshness validator passes.

## Rollback note

Revert the PMF Engine shipping commit to remove the new framework mirrors, benchmark coverage row, generated asset refresh, and task/history updates. If only the public showcase catch-up is problematic, rerun the showcase generators from the previous commit and inspect the generated diff before reverting skill files.

## Next command

`$exec`
