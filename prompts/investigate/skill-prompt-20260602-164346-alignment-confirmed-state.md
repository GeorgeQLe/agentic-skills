---
skill: investigate
agent: codex
captured_at: 2026-06-02T16:43:50-04:00
source: user-invocation
prompt_scope: visible-user-invocation
---

# Visible User Invocation

```text
A previous agent produced the plan below to accomplish the user's task. Implement the plan in a fresh context. Treat the plan as the source of user intent, re-read files as needed, and carry the work through implementation and verification.

# Alignment Page Confirmed-State Contract

**Summary**
- Change the alignment-page lifecycle from a permanent approval form into a two-state contract: `review` before approval and `confirmed` after approval is applied.
- In `review`, keep evidence, gates, feedback YAML, and final compiled answers.
- In `confirmed`, preserve the full research and approval record, apply user-requested updates, remove gate-question UI, and mark the page as finished/current while explicitly allowing future archived amendments.

**Recommended Contract Changes**
- Update the canonical alignment convention in `CLAUDE.md` only, then regenerate bundled `ALIGNMENT-PAGE.md` files with `scripts/upgrade-alignment-page.mjs`.
- Add an **Alignment lifecycle** section:
  - `review`: draft/pre-approval page with required inline questions and compile controls.
  - `confirmed`: post-approval page after the agent has consumed final approval, updated requested content, and written/updated the approved canonical artifacts.
  - `amended`: future revision of a confirmed page when later research changes or extends prior conclusions.
- Add a **Confirmed page contract**:
  - Include a clear status block: `alignment_status: confirmed`, confirmation date, confirmed artifact paths, approval source summary, and “finished but amendable” language.
  - Remove required gate-question controls, final `Compile Answers`, unanswered-question counters, and approval-blocking language.
  - Preserve the answered decisions, user requests, evidence matrix, assumptions/confidence register, source gaps, proposed file changes, and what changed during confirmation.
  - Keep research caveats visible; “confirmed” means approved/current, not immutable or permanently true.
- Add an **After approval handling** rule:
  - If final compiled YAML contains approvals plus requested edits, first update the page/research to satisfy the user request, then confirm it.
  - If final compiled YAML includes `needs-clarification` or unresolved negative feedback, revise and return to `review`, not `confirmed`.
  - Before replacing any existing alignment page, archive it under the existing `docs/history/archive/YYYY-MM-DD/HHMMSS/alignment/...` path.

**Implementation Plan**
- First execution step: capture the visible `$investigate` invocation in `prompts/investigate/` as required by repo policy.
- Update `CLAUDE.md` inside the `alignment-convention` block:
  - Revise `Alignment gates`, `Gate YAML contract`, and `Pre-approval stop` so gates are review-state only.
  - Add the new lifecycle and confirmed-page paragraphs.
  - Clarify that future research amendments archive the previous confirmed page and highlight changes.
- Run `node scripts/upgrade-alignment-page.mjs` to regenerate generated `ALIGNMENT-PAGE.md` bundles.
- Update `tests/layer1/alignment-gates.test.ts`:
  - Keep existing review-state assertions for gates and compile controls.
  - Add assertions that the convention defines `review` and `confirmed` states.
  - Add assertions that confirmed pages remove gate questions/compile controls while preserving approval decisions and evidence.
  - Keep the generator drift guard.
- If regenerated skill bundles or inline contracts require version/archive handling, use `scripts/skill-archive.sh <skill-dir>`, bump only affected substantive skill contracts, and update their `CHANGELOG.md`.
- Preserve the unrelated dirty file `apps/skills-showcase/alignment/animation-code-walkthrough.html` unless showcase regeneration proves it is intentionally part of the change.

**Test Plan**
- `node scripts/upgrade-alignment-page.mjs --dry-run`
- Focused alignment contract test, likely `pnpm --dir tests exec vitest run --project layer1 layer1/alignment-gates.test.ts`
- `bash scripts/skill-versions.sh --missing`
- `bash scripts/skill-archive-audit.sh --strict` if any skill versions are bumped
- `bash scripts/validate-skills-showcase-data.sh` only if generated showcase data is refreshed
- `git diff --check`

**Assumptions**
- “Confirmed” means the user-approved alignment page has been applied and is no longer waiting for gate answers.
- “Finished” means the current research/alignment cycle is complete, not frozen against future research.
- The implementation should not edit existing generated `alignment/*.html` artifacts except any page created for this investigation or files explicitly regenerated by existing repo scripts.
```

```text
<skill>
<name>investigate</name>
<path>/home/georgeqle/projects/tools/dev/agentic-skills/.codex/skills/investigate/SKILL.md</path>
---
name: investigate
description: Validate user claims against codebase and git history, trace to root cause, and propose a fix
type: debugging
version: v0.1
---

# Investigate

Invoke as `$investigate`.

Use this skill when the user reports a bug, error, unexpected behavior, or provides observations/hypotheses about what they think is wrong. Validates claims against the codebase and git history before tracing to root cause.

## Workflow

1. Parse the error message, stack trace, bug description, or user observations/claims.
2. **Select investigation strategy** from flags or auto-detection:
   - `--ui`: Visual/layout bugs — start from component tree, styles, animations, transitions, spacing, theming, skeleton/loading states, positioning. Signal words: skeleton, animation, spacing, popover, transition, theme, truncated, glitch, dead space, detached, pop-in, flickering, layout, z-index, overflow.
   - `--data`: Data/state bugs — start from data pipeline, state management, timers, counters, sync logic. Signal words: countdown, percentage, usage, sync, stale, timer, refresh, reset, missing data, wrong number, out of sync, not updating, mismatch.
   - General (default): Errors, crashes, behavioral bugs, and anything ambiguous — start from symptom/stack trace.
   - If input has signals from both UI and data, prefer data strategy — wrong data commonly manifests as a visual symptom.
3. If the user provides claims or hypotheses, validate each one against the codebase and git history (`git log`, `git diff`, `git blame`). Classify each as confirmed, partially correct, or not supported by evidence. Report findings before proceeding.
4. Trace using the selected strategy:
   - **UI**: component tree → styles → layout → animation/transition → render lifecycle. Check CSS conflicts, missing keys, layout thrashing, transition timing.
   - **Data**: data source → transform → store → subscription → render. Check stale closures, missing dependency arrays, incorrect comparisons, cache invalidation, timer drift.
   - **General**: execution path from symptom to source — null/undefined, wrong types, missing env vars, stale imports, race conditions, schema mismatches.
   - If the initial strategy hits a dead end, pivot to the other — a UI bug may have a data root cause, and vice versa.
5. Check recent git history for changes that may have introduced the issue.
6. Identify the root cause with file and line reference.
7. Apply a minimal fix and write or update tests.
8. Run tests to verify the fix and check for regressions.

## Output Format

- **Strategy Used**: UI / Data / General (auto-detected or flag-forced), whether a pivot occurred
- **User Claims Validated**: For each claim — verdict (confirmed/partially correct/not supported) and evidence. Skip if input was a plain error message.
- **Root Cause**: file:line, what's wrong, when introduced, relationship to user's theory
- **Fix Applied**: files modified, test results
- **Prevention**: what check would have caught this earlier

## Constraints

- Do not refactor unrelated code.
- Validate user claims before assuming they're correct — observations are a starting point, not ground truth.
- If the root cause can't be determined, report what was ruled out.
- Always run tests after applying the fix.


## Alignment Page

When this skill produces durable deliverables (research, specs, plans, reports, prototypes, or any document output), build a full-depth HTML alignment page following `ALIGNMENT-PAGE.md` in this skill's directory. Output: `alignment/investigate-{topic}.html`.

## Default Shipping Contract

Follow the shared shipping contract convention in CLAUDE.md.

</skill>
```
