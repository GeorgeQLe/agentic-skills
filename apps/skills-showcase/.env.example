# Neon Postgres connection string (pooled)
# Get from: https://console.neon.tech → project → Connection Details
DATABASE_URL=postgresql://user:password@ep-example.us-east-2.aws.neon.tech/dbname?sslmode=require

# Secret for admin-only newsletter endpoints (generate a random string)
NEWSLETTER_ADMIN_SECRET=change-me-to-a-random-secret
