---
name: legacy-agent-route-with-npm-invalid
description: Invalid fixture proving agent-native install routes fail even with npm alternatives.
version: v0.0
---

## Pack Availability Guard

When recommending a skill from another pack, recommend `/pack install <pack>` inside Claude Code, or `npx skillpacks install <pack>` from the project shell.
