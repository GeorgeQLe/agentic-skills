interface:
  display_name: "Roadmap"
  short_description: "Build or update the project roadmap from specs and interview"
  default_prompt: "Use $roadmap to build or update the project roadmap by interviewing on priorities and sequencing."

policy:
  allow_implicit_invocation: true
