interface:
  display_name: "Ship End Kanban"
  short_description: "Wrap up session and move kanban card to Done"
  default_prompt: "Use $ship-end-kanban to wrap up the session and move the kanban card to Done."

policy:
  allow_implicit_invocation: true
